#include <stdio.h>

int main(){
    int kg;
    double precoFinal;
    double morango;
    double maca;

    printf("vc quer quantos kilos de maça");
    scanf("%lf",&maca);
    printf("vc quer quantos kilos de morango");
    scanf("%lf",&morango);

    if (kg <= 5)
    {
        maca = maca * 2.50;
        morango = morango * 2.50;
        precoFinal = morango +

    }
        
}